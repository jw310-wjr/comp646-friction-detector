# Multimodal VLM fusion — requires Python >= 3.10. Use with: pip install -r requirements-pipeline.txt -r requirements.txt
# Vision-language fusion (see https://huggingface.co/Qwen/Qwen2.5-VL-7B-Instruct)
git+https://github.com/huggingface/transformers
accelerate
torch
torchvision
qwen-vl-utils==0.0.8

# Vision stream
opencv-python-headless>=4.8.0
numpy>=1.24.0
deepface>=0.0.90
tensorflow>=2.15.0

# Language stream (Whisper — proposal; faster-whisper is faster on CPU)
faster-whisper>=1.0.0

# Utilities
tqdm>=4.66.0
imageio-ffmpeg>=0.4.9

# Stretch: board OCR (optional)
# pytesseract>=0.3.10
